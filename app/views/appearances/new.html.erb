<h1>Book an Appearance</h1>

<% if flash[:errors] %>
    <%= flash[:errors] %>
<% end %>

<%= form_for @appearance do |f|%>
    <%= f.label :guest %>
    <%= f.collection_select :guest_id, Guest.all, :id, :name%> 

    <%= f.label :appearance %>
    <%= f.collection_select :episode_id, Episode.all, :id, :number%>

    <%= f.label :rating %>
    <%= f.number_field :rating%>
    <%= f.submit %>
<%end %>